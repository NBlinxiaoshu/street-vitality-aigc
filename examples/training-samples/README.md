# 训练数据格式示例

本目录保留 5 组 PNG + 同名 TXT，用于展示 LoRA 训练数据的文件组织、空间类型与 Caption 格式。完整训练集未上传，原因见 [`../../docs/data-card.md`](../../docs/data-card.md)。

示例 Caption 由触发词 `street_vitality` 与封闭标签组成；视觉模型只负责初标，最终标签由人工复核。
